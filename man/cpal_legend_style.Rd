% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactive.R
\name{cpal_legend_style}
\alias{cpal_legend_style}
\title{CPAL Legend Style for mapgl}
\usage{
cpal_legend_style(
  theme = c("light", "dark"),
  position = c("bottom-right", "bottom-left", "top-right", "top-left"),
  width = 180
)
}
\arguments{
\item{theme}{Legend theme: "light" (default) or "dark"}

\item{position}{Legend position: "top-left", "top-right", "bottom-left",
or "bottom-right" (default)}

\item{width}{Legend width in pixels or "auto". Default is 180.}
}
\value{
A named list of style options to pass to \code{add_legend(style = ...)}
}
\description{
Returns a style list for use with \code{mapgl::add_legend()}. Provides
consistent CPAL branding for map legends with light and dark theme options.
}
\details{
The style list includes:
\itemize{
  \item Background color and opacity
  \item Border styling
  \item Typography (Inter font family)
  \item Shadow for visual depth
  \item Consistent sizing and spacing
}
}
\examples{
\dontrun{
library(mapgl)

cpal_mapgl() |>
  add_fill_layer(
    id = "counties",
    source = nc,
    fill_color = color_scale$expression
  ) |>
  add_legend(
    "Poverty Rate (\%)",
    values = c("0-10", "10-20", "20-30"),
    colors = c("#E8F4F6", "#28889E", "#004855"),
    style = cpal_legend_style()
  )

# Dark theme legend
cpal_mapgl(theme = "dark") |>
  add_fill_layer(...) |>
  add_legend(
    "Value",
    values = values,
    colors = colors,
    style = cpal_legend_style(theme = "dark")
  )
}

}
