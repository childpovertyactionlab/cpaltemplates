% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highcharter.R
\name{hc_add_cpal_logo}
\alias{hc_add_cpal_logo}
\title{Add CPAL Logo to Highcharter Chart}
\usage{
hc_add_cpal_logo(
  hc,
  logo_url = NULL,
  mode = "light",
  position = "top-right",
  width = 60,
  height = NULL,
  opacity = 0.5,
  margin = 10
)
}
\arguments{
\item{hc}{A highchart object}

\item{logo_url}{URL or base64 string of the logo image. If NULL, automatically
loads the appropriate CPAL logo from cpaltemplates package.}

\item{mode}{Theme mode: "light" or "dark". Determines which logo variant to use
when logo_url is NULL. Default is "light" (teal logo).}

\item{position}{Logo position: "top-right" (default), "top-left", "bottom-right", "bottom-left"}

\item{width}{Logo width in pixels (default: 60)}

\item{height}{Logo height in pixels (default: auto based on 370:250 aspect ratio)}

\item{opacity}{Logo opacity 0-1 (default: 0.5)}

\item{margin}{Margin from chart edge in pixels (default: 10)}
}
\value{
A highchart object with logo added
}
\description{
Adds the CPAL logo to a highchart using the renderer.image() method.
Logo is positioned in the top-right corner by default.
}
\examples{
\dontrun{
# Light mode (teal logo, top-right by default)
hchart(data, "bar", hcaes(x, y)) |>
  hc_add_theme(hc_theme_cpal_light()) |>
  hc_add_cpal_logo()

# Dark mode (white logo)
hchart(data, "bar", hcaes(x, y)) |>
  hc_add_theme(hc_theme_cpal_dark()) |>
  hc_add_cpal_logo(mode = "dark")
}
}
