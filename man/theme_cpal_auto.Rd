% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes.R
\name{theme_cpal_auto}
\alias{theme_cpal_auto}
\title{CPAL Theme with Thematic Auto-theming Support}
\usage{
theme_cpal_auto(
  base_size = 14,
  base_family = "",
  base_line_size = base_size/22,
  base_rect_size = base_size/22,
  grid = c("horizontal", "vertical", "both", "none"),
  axis_line = c("x", "y", "both", "none"),
  axis_title = TRUE,
  legend_position = "bottom"
)
}
\arguments{
\item{base_size}{Base font size (default: 16)}

\item{base_family}{Base font family (default: uses Inter from Google Fonts if available)}

\item{base_line_size}{Base size for line elements (default: base_size/22)}

\item{base_rect_size}{Base size for rect elements (default: base_size/22)}

\item{grid}{Grid lines: "horizontal", "vertical", "both", or "none" (default: "horizontal")}

\item{axis_line}{Axis line position: "x", "y", "both", or "none" (default: "x")}

\item{axis_title}{Include axis titles (default: TRUE)}

\item{legend_position}{Legend position (default: "bottom")}
}
\value{
A ggplot2 theme object compatible with thematic auto-theming
}
\description{
A variant of theme_cpal() designed for use with the thematic package.
Applies CPAL styling for fonts, sizes, and spacing while allowing
thematic to control background and text colors for automatic
light/dark mode support in Shiny apps.
}
\details{
This is a convenience wrapper around `theme_cpal(thematic = TRUE)`.
}
\examples{
\dontrun{
library(shiny)
library(thematic)

# In server function:
thematic::thematic_on()

output$my_plot <- renderPlot({
  ggplot(data, aes(x, y)) +
    geom_point() +
    theme_cpal_auto()
})
}
}
\seealso{
[theme_cpal()] for the standard theme with explicit colors
}
