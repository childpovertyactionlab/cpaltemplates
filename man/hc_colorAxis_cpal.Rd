% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highcharter.R
\name{hc_colorAxis_cpal}
\alias{hc_colorAxis_cpal}
\title{Apply CPAL Color Axis for Heatmaps}
\usage{
hc_colorAxis_cpal(
  hc,
  palette = "sequential",
  min = NULL,
  max = NULL,
  reverse = FALSE,
  stops = NULL
)
}
\arguments{
\item{hc}{A highchart object}

\item{palette}{Palette type: "sequential" (default) or "diverging"}

\item{min}{Minimum value for color scale (default: NULL, auto-calculated)}

\item{max}{Maximum value for color scale (default: NULL, auto-calculated)}

\item{reverse}{Reverse the color direction (default: FALSE)}

\item{stops}{Number of color stops to use (default: uses full palette)}
}
\value{
A highchart object with colorAxis configured
}
\description{
Returns a pre-configured colorAxis using CPAL color palettes.
Useful for heatmaps, choropleths, and other continuous color scales.
}
\examples{
\dontrun{
# Sequential (good for heatmaps with positive values)
hchart(data, "heatmap", hcaes(x, y, value = z)) |>
  hc_colorAxis_cpal("sequential", min = 0, max = 100)

# Diverging (good for correlation matrices, -1 to 1)
hchart(cor_data, "heatmap", hcaes(x, y, value = correlation)) |>
  hc_colorAxis_cpal("diverging", min = -1, max = 1)
}
}
